for(let i=0; i<list.length; i++){
      let name = list[i].name;
      let url = list[i].url;
      let primary = list[i].primary;
      let secondary = list[i].secondary;

      const ulContainer = document.createElement('ul');
      ulContainer.id = 'team-list-nav';
      ulContainer.className = 'team-links';

      const liContainer = document.createElement('li');
      liContainer.style.backgroundColor = primary;
      ulContainer.appendChild(liContainer);

      const aContainer = document.createElement('a');
      aContainer.href = url;
      aContainer.style.color = secondary;
      liContainer.appendChild(aContainer);

      const spanContainer = document.createElement('span');
      spanContainer.textContent = [copy];
      liContainer.appendChild(spanContainer);
   }
   document.body.appendChild(ulContainer);

   